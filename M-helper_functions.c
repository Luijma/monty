/**
 * process_line - adds commands and parameters to array
 * @line - line to be tokenized
 */
char **process_line(char *line, size_t line_buf_size)
{
	char *token;
	char **input_array;
	int idx;

	input_array = malloc(sizeof(input_array) * 2);
	
}
